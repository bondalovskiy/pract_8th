server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pract_8th_order-service
    username: postgres
    password: bondalovskiy
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: false