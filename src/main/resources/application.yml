spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_pract_8th
    username: postgres
    password: bondalovskiy
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true